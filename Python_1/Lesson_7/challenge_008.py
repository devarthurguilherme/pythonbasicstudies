meter = int(input('Digite the value in meters: '))
centimeter = meter * 100
millimeter = centimeter * 10

print('You digited {} meter(s), so it in centimeters is {} and milimeters is {}'.format(meter, centimeter, millimeter))